# UI 测试需求文档模板（5 节）

> 用途：作为 `ui-requirements-analyze` / `ui-scenario-design` 的固定输入，业务方只需填写本模板，AI 据此生成项目绑定配置与测试场景。
> 存放路径：`tests/{mode}/_workflow/00-requirements/ui-requirements.md`（基线模式 `tests/baseline/...`）
> 结构：**5 节固定结构**（与 `00-requirements/ui-requirements-TEMPLATE.md` 保持一致），各节按需扩展子表格/子段落。

## 1. 模块与版本

| 模块名 | 版本 | 业务负责人 | 日期 |
|--------|------|-----------|------|
| {模块A} | {v1.0} | {负责人} | {YYYY-MM-DD} |

## 2. 业务流程（叙述，每流程一段）

[业务用自然语言描述用户操作路径与预期结果]

### {流程 1：模块A 核心流程}

1. {操作步骤}
2. {预期结果}

## 3. 数据变更映射（必填）

| 业务操作 | 涉及表（业务命名） | 变更类型 | 数据变化描述 | 关键字段与取值 | 业务反馈（可断言文案） |
|---------|------------------|---------|-------------|---------------|----------------------|
| 新增图书 | 图书表 | 新增 | 新增 1 条记录 | 标题=表单"书名"；库存=0；状态=启用 | 「新增成功」 |
| 借出图书 | 图书表 / 借阅表 | 修改+新增 | 图书表库存-1；借阅表+1 条 | 库存=原值-1；借阅状态=进行中 | 「借阅成功」 |

> 「涉及表」为业务命名，物理表名由 `ui-requirements-analyze` 对照后端 schema 校验后确定；断言语义以「关键字段与取值」列为准。

## 4. 字段明细（复杂场景补充）

- {表名（业务命名）}
  - `{字段}`：{来源，如来自表单"书名"输入框，新增必填}
  - `{字段}`：{取值规则，如新增初始 0；借出时减 1}
  - `{字段}`：{枚举，如 {启用,停用}}

## 5. 数据约束（可选）

- 唯一约束：{如 图书编码不可重复}
- 种子依赖：{如 删除需先停用（复用已有种子数据）}
- 状态机：{如 草稿 → 已发布 → 归档}
- 删除语义：{物理删除 / 软删除 / 停用}