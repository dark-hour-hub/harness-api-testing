# 增量测试需求

> **基准分支**: `<基准分支，如 main>`
> **生成时间**: `<YYYY-MM-DD>`
> **源码项目**: `<项目名称>` (`<源码路径>`)
> **基线路径**: `tests/baseline/_workflow`
>
> 本文档基于 `git diff` + CodeGraph 影响链分析生成。开发者可手动修改。

---

## 1. 变更概要

| 维度 | 数量 | 说明 |
|------|:----:|------|
| 新增接口 | `<n>` | |
| 修改接口 | `<n>` | |
| 删除接口 | `<n>` | |
| 新增/修改实体字段 | `<n>` | |
| 认证变更 | `<是/否>` | |
| 框架变更 | `<是/否>` | |

---

## 2. 接口变更

<!--
  每节一个接口，编号从 2.1 开始递增。
  标题格式：METHOD PATH — 简述 `变更类型标签`

  变更类型标签（三者选一）：
  - `新增` — change_type=add
  - `修改` — change_type=update
  - `删除` — change_type=delete

  测试要点分类（参照 testPoint-interface.md）：
  正向功能 / 参数校验-必填 / 参数校验-空值 / 参数校验-格式 / 参数校验-类型
  / 参数校验-边界值 / 参数校验-枚举值 / 参数校验-默认值 / 参数校验-特殊字符
  / 参数校验-组合 / 异常处理 / 权限验证 / 幂等性 / 并发 / 兼容性
  / 响应字段变更 / 数据一致性 / 回归
-->

### 2.1 `<METHOD> <PATH>` — `<简述>` `<变更类型标签>`

| 属性 | 值 |
|------|-----|
| **变更类型** | `<add / update / delete>` |
| **所属模块** | `<模块名>` |
| **Controller** | `<ClassName.methodName()>` |
| **变更说明** | `<变更原因和影响简述>` |
| **影响已有用例** | `<受影响的 case ID，多个用逗号分隔；新增接口填"无">` |

**测试要点**：

| # | 分类 | 优先级 | 测试点 | 预期结果 |
|---|------|:---:|------|------|
| 1 | `<分类>` | `<P0/P1/P2>` | `<操作描述>` | `<预期响应，如 HTTP 302 / body_contains "xxx">` |
| 2 | `<分类>` | `<P0/P1/P2>` | `<操作描述>` | `<预期响应>` |

---

### 2.2 `<METHOD> <PATH>` — `<简述>` `<变更类型标签>`

| 属性 | 值 |
|------|-----|
| **变更类型** | `<add / update / delete>` |
| **所属模块** | `<模块名>` |
| **Controller** | `<ClassName.methodName()>` |
| **变更说明** | `<变更原因和影响简述>` |
| **影响已有用例** | `<case IDs 或 无>` |

**测试要点**：

| # | 分类 | 优先级 | 测试点 | 预期结果 |
|---|------|:---:|------|------|
| 1 | `<分类>` | `<P0/P1/P2>` | `<操作描述>` | `<预期响应>` |

---

<!-- 删除接口的写法（不需要测试要点表） -->

### 2.X `<METHOD> <PATH>` — `<简述>` `删除`

| 属性 | 值 |
|------|-----|
| **变更类型** | `delete` |
| **所属模块** | `<模块名>` |
| **变更说明** | `<删除原因>` |
| **影响已有用例** | `<case IDs>` |
| **处理方式** | 删除以上用例 |

---

## 3. 实体字段变更

<!-- 无变更时写"无" -->

### 3.1 `<EntityName>.<fieldName>`

| 字段 | 变更类型 | 类型 | 变更前 | 变更后 |
|------|:---:|------|------|------|
| `<field>` | `<add / modified / delete>` | `<类型>` | `<旧定义>` | `<新定义>` |

**受影响接口**：

| 接口 | 影响方式 | 说明 |
|------|:---:|------|
| `<METHOD PATH>` | `<请求体校验 / 响应体 / 请求体+响应体>` | `<简述>` |

<!-- 若测试要点已在第 2 节覆盖，可加备注跳过 -->
> 已包含在第 2 节接口变更中，此处不再重复列出测试要点。

<!-- 若测试要点未在第 2 节覆盖，则展开：
**测试要点**：

| # | 分类 | 优先级 | 测试点 | 预期结果 |
|---|------|:---:|------|------|
| 1 | ... | P1 | ... | ... |
-->

---

## 4. Service 层变更（间接影响）

<!-- 无变更时写"无" -->

> 本次变更无 Service 层代码变更。

<!-- 有变更时：
| Service | 变更方法 | 受影响 API | 影响说明 | 建议 |
|------|------|------|------|------|
| `<ClassName>` | `<method>` | `<METHOD PATH>` | `<简述>` | 回归 |
-->

---

## 5. 认证变更

<!-- 无变更时写"无" -->

> 无。

<!-- 有变更时展开说明 -->
<!--
| 属性 | 变更前 | 变更后 |
|------|------|------|
| **认证类型** | ... | ... |
| **必填请求头** | ... | ... |

**受影响接口**：...

**测试要点**：...
-->

---

## 6. 框架/配置变更

<!-- 无变更时写"无" -->

> 无。

<!-- 有变更时展开说明 -->
<!--
| 变更项 | 变更前 | 变更后 | 测试影响 |
|------|------|------|------|
| ... | ... | ... | ... |
-->

---

## 7. 回归清单

<!--
  未直接变更但需回归确认的接口。
  来源枚举：
  - same_module: 同模块其他接口
  - service_chain: Service/Model 调用链影响
  - smoke: 冒烟基线（每次必跑）
-->

| 接口 | 回归原因 | 来源 |
|------|------|:---:|
| `<METHOD PATH>` | `<原因>` | `<same_module / service_chain / smoke>` |

---

## 8. 废弃用例

<!-- 无废弃用例时写"无" -->

| 用例 ID | 废弃原因 |
|------|------|
| `<TC_XXX>` | `<原因，如对应接口已删除>` |

---

## 附录：变更源码对照

<!-- 每条变更用一个小节，标注文件路径和行号 -->

### A. `<FileName.java>` (`<完整路径>`)

```diff
<git diff hunk — 直接粘贴关键变更行>
```

### B. `<FileName.java>` (`<完整路径>`)

```diff
<git diff hunk>
```

### 影响链

```
<变更符号或方法>
  └── <受影响类或方法>
       ├── <METHOD PATH> → <影响简述>
       └── <METHOD PATH> → <影响简述>
```
