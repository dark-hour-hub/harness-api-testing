# {ClassName} — {实体类型}

## 基本信息

| 属性 | 值 |
|------|-----|
| **类名** | `{ClassName}` |
| **全限定名** | `{com.example.xxx.dto.XxxDto}` |
| **类型** | {请求体 DTO / 响应体 VO / 业务对象 BO} |
| **所属模块** | {模块名称} |
| **父类** | {无 / `BaseEntity` / 其他} |
| **序列化特性** | {`@JsonInclude(NON_NULL)` / 无特殊配置} |
| **说明** | {类级别注解说明 / 业务用途} |

---

## 注解转义说明

> 仅列出 Java 字段名与 JSON 键名不一致、或有额外显示名注解的字段。若所有字段均无转义，删除此段。

| Java 字段名 | JSON 键名 | 显示名 | 转义来源 |
|------------|----------|--------|----------|
| `{javaField}` | `{jsonKey}` | {显示名} | `@JsonProperty("{jsonKey}")` |
| `{javaField}` | `{jsonKey}` | {显示名} | `@SerializedName("{jsonKey}")` |
| `{javaField}` | `{jsonKey}` | {显示名} | 类级 `@JsonNaming(SnakeCaseStrategy.class)` |
| `{javaField}` | — | {显示名} | `@ApiModelProperty(value="{显示名}")`（仅显示名，未改键名） |

---

## 字段定义

### {请求体 / 响应体}字段

| 字段路径 | JSON 键名 | 类型 | 必填 | 约束/校验规则 | 默认值 | 说明 | 示例值 |
|----------|-----------|------|:---:|------------|--------|------|--------|
| {field} | `{jsonKey}` | {String} | ✅ | `@NotBlank`, 长度 2-30 | — | {说明} | `"example"` |
| {field} | `{jsonKey}` | {Long} | ✅ | `@NotNull` | — | {说明} | `1` |
| {field} | `{jsonKey}` | {String} | 否 | `@Email` | — | {说明} | `"test@example.com"` |
| {field} | `{jsonKey}` | {String} | 条件 | — | — | {条件说明} | — |
| {child}.{sub} | `{jsonKey}` | {Type} | {✅/否} | {约束} | — | {嵌套对象字段说明} | `"example"` |

**字段备注**（无则删除）:
- `{field}`: {特殊说明 — 如 `@JsonIgnore` + `@JsonProperty` 同时存在、`@Sensitive` 脱敏、`@Translation` 翻译等}

---

## JSON 示例

```json
{
  "jsonKey1": "value1",
  "jsonKey2": 123,
  "nested": {
    "subKey": "subValue"
  }
}
```

---

*基于 `{全限定名}` 源码生成 · {YYYY-MM-DD}*
